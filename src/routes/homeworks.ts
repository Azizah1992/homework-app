import { FastifyInstance } from 'fastify';

export default async function (server: FastifyInstance) {
	server.get('/azizah', async (request, reply) => {
		// add your code here
	});

	server.get('/Homework/azizah', async (request, reply) => {
		// add your code here
	});
}
